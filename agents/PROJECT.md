# Project Notes (Atago)

- Scope: unit and property management.
- Entry: `src/index.ts`; core under `src/core`.
- Tests: `npm test --workspace=Atago` (Vitest).
- Build: `npm run build --workspace=Atago`.
