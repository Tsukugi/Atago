# Agents Guide (Atago)

- Tests: `npm test --workspace=Atago`
- Build: `npm run build --workspace=Atago`
- Lint/format: `npm run lint --workspace=Atago`
- Entry: `src/index.ts` (units/properties); keep types strict (avoid `any`).
